# 規格文件：移除 git_commit_push 工具

**版本：** 1.0.0
**狀態：** 規劃中
**建立日期：** 2025-11-13
**優先級：** 低
**估計工時：** 0.5-1 天

## 1. 功能名稱

**移除 git_commit_push MCP 工具** - 移除 Git 操作工具，簡化 MCP 伺服器功能

## 2. 問題陳述

### 目前狀況

i18n MCP 翻譯器目前提供 7 個 MCP 工具：

**保留的工具（6 個）**
1. ✅ `translate-file` - 翻譯單一原始檔案
2. ✅ `enhanced_translate_file` - 增強版翻譯（快取）
3. ✅ `batch_translate_files` - 批次翻譯多個檔案
4. ✅ `generate_locale_diff` - 產生分支差異檔案
5. ✅ `merge_translations` - 合併已審核的翻譯
6. ✅ `cleanup_diff_directory` - 清理差異目錄

**需要移除的工具（1 個）**
7. ❌ `git_commit_push` - Git 提交和推送操作

### 為什麼要移除 git_commit_push？

#### 1. **Git 操作過於基礎**
- `git_commit_push` 只是包裝了 `git add`、`git commit`、`git push` 指令
- 開發人員已經非常熟悉這些 git 指令
- 提供這個工具沒有增加顯著價值

#### 2. **其他工具已經整合 Git 功能**
- `generate_locale_diff` 已經有 `autoCommit` 和 `autoPush` 參數
- `merge_translations` 已經有 `autoCommit` 和 `autoPush` 參數
- 這些整合的 Git 功能更符合工作流程脈絡

#### 3. **安全性考量**
- 自動化 git 操作可能導致意外的提交或推送
- 開發人員應該保持對版本控制的完全控制
- 明確的 git 指令操作更安全、更可預測

#### 4. **減少 API 表面積**
- 一個獨立的 git 工具增加了 API 複雜度
- 使用者需要學習額外的工具參數
- 移除後使 MCP 伺服器更專注於翻譯相關功能

#### 5. **可替代性高**
- 使用標準 git 指令可以輕鬆替代
- 不需要額外的學習成本
- 更靈活（可以使用完整的 git 功能）

### 保留其他操作工具的原因

#### `generate_locale_diff` - 保留 ✅
- **提供獨特價值**：自動比較分支並產生結構化差異
- **難以手動替代**：需要複雜的 git diff 解析和 JSON 處理
- **符合工作流程**：翻譯審核流程的重要部分

#### `merge_translations` - 保留 ✅
- **提供獨特價值**：智慧合併翻譯，保留未變更的金鑰
- **難以手動替代**：需要複雜的 JSON 比對和合併邏輯
- **符合工作流程**：審核後整合的關鍵步驟

#### `cleanup_diff_directory` - 保留 ✅
- **提供方便性**：與 `merge_translations` 配合使用
- **可選功能**：merge_translations 可以自動呼叫
- **簡單但實用**：清理工作流程產生的暫存檔案

## 3. 提議解決方案

### 3.1 移除範圍

#### 完全移除的工具（1 個）
- ❌ `git_commit_push` - Git 提交推送工具

#### 保留的工具（6 個）
- ✅ `translate-file` - 核心翻譯功能
- ✅ `enhanced_translate_file` - 增強翻譯（快取）
- ✅ `batch_translate_files` - 批次翻譯
- ✅ `generate_locale_diff` - 產生差異（含整合的 git 功能）
- ✅ `merge_translations` - 合併翻譯（含整合的 git 功能）
- ✅ `cleanup_diff_directory` - 清理差異目錄

### 3.2 實施方法

#### 階段 1：移除 git_commit_push 工具
1. 從 `src/server/mcp-tools.ts` 移除工具註冊
2. 移除 `import { setupGitCommitPushTool }` 語句
3. 刪除或封存 `src/tools/git-commit-push.ts` 檔案

#### 階段 2：更新文件
1. 更新 `README.md`：移除 git_commit_push 章節
2. 更新 `CLAUDE.md`：更新工具清單
3. 更新 `.specify/specs/現有功能規格.md`：標記為已移除
4. 建立簡短的遷移說明

#### 階段 3：版本更新
1. 更新 `package.json` 版本號（建議：1.3.0 - 次要版本）
2. 更新 `CHANGELOG.md` 說明變更
3. 在 README 中標註此變更

### 3.3 替代方案指南

為使用者提供簡單的替代方式：

#### 替代 `git_commit_push`

**原本使用 MCP 工具：**
```json
{
  "tool": "git_commit_push",
  "params": {
    "files": ["src/assets/locale/zh-TW.json"],
    "commitMessage": "i18n: update translations",
    "push": true,
    "branch": "feature-branch"
  }
}
```

**改用標準 git 指令：**
```bash
# 新增檔案
git add src/assets/locale/zh-TW.json

# 提交變更
git commit -m "i18n: update translations"

# 推送到遠端
git push origin feature-branch
```

**或使用其他工具的整合功能：**
```json
{
  "tool": "generate_locale_diff",
  "params": {
    "localeDir": "src/assets/locale",
    "autoCommit": true,
    "commitMessage": "i18n: generate diff for review",
    "autoPush": true
  }
}
```

### 3.4 保留的功能

移除 `git_commit_push` 後，所有核心功能保持不變：

**翻譯功能（完整保留）**
- ✅ 自動偵測硬編碼文字
- ✅ AI 產生脈絡金鑰
- ✅ 多語言翻譯
- ✅ 程式碼重構
- ✅ 更新語言檔案
- ✅ 快取和最佳化
- ✅ 批次處理

**工作流程功能（完整保留）**
- ✅ 產生分支差異（含整合的 git commit/push）
- ✅ 合併已審核翻譯（含整合的 git commit/push）
- ✅ 清理暫存目錄

## 4. 需求

### 4.1 功能需求

#### FR1：移除工具註冊
- **FR1.1**：從 `mcp-tools.ts` 移除 `git_commit_push` 工具註冊
- **FR1.2**：移除 `setupGitCommitPushTool` 的 import 語句
- **FR1.3**：確保移除後伺服器仍能正常啟動
- **FR1.4**：驗證其他 6 個工具仍正常運作

#### FR2：移除工具檔案
- **FR2.1**：刪除 `src/tools/git-commit-push.ts` (3,174 bytes)
- **FR2.2**：使用 git rm 保留檔案歷史
- **FR2.3**：可選：移至 `archived/` 目錄作為參考

#### FR3：更新文件
- **FR3.1**：更新 `README.md`：
  - 移除 git_commit_push 章節
  - 更新工具數量（從 7 個改為 6 個）
  - 新增簡短的替代說明
- **FR3.2**：更新 `CLAUDE.md`：
  - 移除 git_commit_push 文件
  - 更新可用工具清單
- **FR3.3**：更新規格文件：
  - 標記 git_commit_push 已移除
  - 說明移除原因
- **FR3.4**：更新 `CHANGELOG.md`

#### FR4：版本管理
- **FR4.1**：更新 `package.json` 版本為 1.3.0（次要版本變更）
- **FR4.2**：更新 `CHANGELOG.md` 新增條目
- **FR4.3**：建立 git 提交說明變更

### 4.2 非功能需求

#### NFR1：向後相容性
- **NFR1.1**：這是次要版本變更，不是重大變更
- **NFR1.2**：核心翻譯功能完全不受影響
- **NFR1.3**：工作流程工具（diff、merge、cleanup）保持不變
- **NFR1.4**：現有使用核心功能的使用者無需任何變更

#### NFR2：文件完整性
- **NFR2.1**：所有文件使用繁體中文（遵循憲章）
- **NFR2.2**：提供清晰的替代方案說明
- **NFR2.3**：更新所有相關文件
- **NFR2.4**：確保文件與程式碼一致

#### NFR3：程式碼品質
- **NFR3.1**：執行 `npm run typecheck` 確保無錯誤
- **NFR3.2**：執行 `npm run build` 確保可編譯
- **NFR3.3**：移除未使用的相依性（如果有）
- **NFR3.4**：清理相關的 TypeScript 類型定義

#### NFR4：測試
- **NFR4.1**：驗證其他 6 個工具正常運作
- **NFR4.2**：使用 MCP Inspector 測試伺服器
- **NFR4.3**：確認沒有破壞現有功能
- **NFR4.4**：測試整合的 git 功能（在 diff 和 merge 工具中）

### 4.3 相依性

#### 檢查相依性
- 檢查 `git-commit-push.ts` 是否有獨特的相依性
- 如果有僅被此工具使用的套件，可以移除
- 其他工具使用的套件必須保留

#### 保留的相依性
所有其他功能需要的套件完全保留。

## 5. 技術設計

### 5.1 檔案變更清單

#### 需要修改的檔案

**src/server/mcp-tools.ts**
```typescript
// 移除這個 import
- import { setupGitCommitPushTool } from '../tools/git-commit-push.js';

// 移除這個註冊呼叫（第 57-58 行）
- // Register git commit push tool
- setupGitCommitPushTool(refreshedServer, this.config, this.translationConfig);
```

#### 需要刪除的檔案
```
src/tools/git-commit-push.ts       (3,174 bytes)
```
**移除大小：** ~3 KB

#### 需要更新的文件
```
README.md                               (移除 git_commit_push 章節)
CLAUDE.md                               (移除工具文件)
.specify/specs/現有功能規格.md          (標記已移除)
package.json                            (版本號 → 1.3.0)
CHANGELOG.md                            (新增條目)
```

### 5.2 更新後的工具清單

```
┌─────────────────────────────────────────────────────────────┐
│                      MCP 工具 (6 個)                         │
├─────────────────────────────────────────────────────────────┤
│ 核心翻譯工具 (3 個)                                         │
│  1. ✅ translate-file           - 基本翻譯                  │
│  2. ✅ enhanced_translate_file  - 增強翻譯（快取）          │
│  3. ✅ batch_translate_files    - 批次翻譯                  │
├─────────────────────────────────────────────────────────────┤
│ 工作流程工具 (3 個)                                         │
│  4. ✅ generate_locale_diff     - 產生差異（含 git 整合）  │
│  5. ✅ merge_translations       - 合併翻譯（含 git 整合）  │
│  6. ✅ cleanup_diff_directory   - 清理差異目錄              │
├─────────────────────────────────────────────────────────────┤
│ Git 工具 (0 個)                                              │
│  7. ❌ git_commit_push          - 已移除                    │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 實施步驟

#### 步驟 1：準備工作
```bash
# 1. 建立功能分支
git checkout -b feature/remove-git-commit-push

# 2. 確保目前狀態可以建置
npm run build
npm run typecheck
```

#### 步驟 2：移除工具註冊
```bash
# 編輯 src/server/mcp-tools.ts
# - 移除 setupGitCommitPushTool 的 import
# - 移除工具註冊呼叫
```

#### 步驟 3：刪除工具檔案
```bash
git rm src/tools/git-commit-push.ts
```

#### 步驟 4：驗證建置
```bash
npm run typecheck  # 應該沒有錯誤
npm run build      # 應該成功編譯
```

#### 步驟 5：測試其他工具
```bash
npm run inspector
# 測試所有 6 個保留的工具
# 確認 git_commit_push 不再出現
```

#### 步驟 6：更新文件
```bash
# 編輯以下檔案：
# - README.md (移除 git_commit_push 章節)
# - CLAUDE.md (移除工具文件)
# - .specify/specs/現有功能規格.md (標記已移除)
# - package.json (版本 → 1.3.0)
# - CHANGELOG.md (新增條目)
```

#### 步驟 7：提交變更
```bash
git add .
git commit -m "feat: 移除 git_commit_push 工具

移除獨立的 git_commit_push 工具，原因：
- Git 操作過於基礎，開發人員已熟悉標準 git 指令
- generate_locale_diff 和 merge_translations 已整合 git 功能
- 簡化 API 表面積，專注於翻譯相關功能

替代方案：
- 使用標準 git 指令 (git add, git commit, git push)
- 使用其他工具的整合 git 功能 (autoCommit, autoPush)

保留的工具 (6 個)：
- translate-file, enhanced_translate_file, batch_translate_files
- generate_locale_diff, merge_translations, cleanup_diff_directory
"
```

#### 步驟 8：推送變更
```bash
git push origin feature/remove-git-commit-push
```

## 6. 測試策略

### 6.1 驗證測試

#### 測試 1：伺服器啟動
```bash
npm run build
npm run start
# 預期：伺服器正常啟動，沒有錯誤
```

#### 測試 2：工具清單
```bash
npm run inspector
# 預期：顯示 6 個工具
# ✅ translate-file
# ✅ enhanced_translate_file
# ✅ batch_translate_files
# ✅ generate_locale_diff
# ✅ merge_translations
# ✅ cleanup_diff_directory
# ❌ git_commit_push (不應出現)
```

#### 測試 3：核心翻譯功能
```bash
# 測試每個核心翻譯工具
# 1. translate-file - 應該正常翻譯
# 2. enhanced_translate_file - 應該使用快取
# 3. batch_translate_files - 應該批次處理
```

#### 測試 4：工作流程工具
```bash
# 測試每個工作流程工具
# 4. generate_locale_diff - 應該產生差異
# 5. merge_translations - 應該合併翻譯
# 6. cleanup_diff_directory - 應該清理目錄
```

#### 測試 5：整合的 Git 功能
```bash
# 測試 generate_locale_diff 的 autoCommit/autoPush
{
  "tool": "generate_locale_diff",
  "params": {
    "localeDir": "src/assets/locale",
    "autoCommit": true,
    "autoPush": true
  }
}
# 預期：成功產生差異並自動提交推送

# 測試 merge_translations 的 autoCommit/autoPush
{
  "tool": "merge_translations",
  "params": {
    "originalDir": "src/assets/locale",
    "reviewedDir": "src/assets/locale/diff",
    "autoCommit": true,
    "autoPush": true
  }
}
# 預期：成功合併並自動提交推送
```

### 6.2 回歸測試

#### 測試 6：所有現有功能保持不變
- ✅ 硬編碼文字偵測
- ✅ AI 金鑰產生
- ✅ 多語言翻譯
- ✅ 程式碼重構
- ✅ 語言檔案更新
- ✅ 快取功能
- ✅ 批次處理
- ✅ 差異產生
- ✅ 翻譯合併
- ✅ 目錄清理

### 6.3 文件測試

#### 測試 7：文件完整性
- ✅ README.md 不再提及 git_commit_push
- ✅ CLAUDE.md 已更新工具清單
- ✅ 所有文件使用繁體中文
- ✅ 沒有遺留的 git_commit_push 參考
- ✅ 提供清晰的替代方案說明

## 7. 文件更新

### 7.1 README.md 變更

#### 移除章節
```markdown
❌ 移除「4. `git_commit_push` - Git Operations」整個章節
```

#### 更新章節
```markdown
📝 更新「Features」：從 7 個工具改為 6 個

📝 更新「Available MCP Tools」：
- 1. translate-file
- 2. enhanced_translate_file
- 3. batch_translate_files
- 4. generate_locale_diff (包含 autoCommit/autoPush)
- 5. merge_translations (包含 autoCommit/autoPush)
- 6. cleanup_diff_directory
```

#### 新增說明
```markdown
📝 在適當位置說明：
- generate_locale_diff 和 merge_translations 已包含 git 整合功能
- 如需獨立的 git 操作，請使用標準 git 指令
```

### 7.2 CLAUDE.md 變更

#### 更新工具清單
```markdown
## Available MCP Tools

### 1. `translate-file` - Source Code Translation
（保留現有說明）

### 2. `enhanced_translate_file` - Enhanced Translation
（保留現有說明）

### 3. `batch_translate_files` - Batch Translation
（保留現有說明）

### 4. `generate_locale_diff` - Compare Branch Changes
（保留現有說明，強調 autoCommit/autoPush 功能）

### 5. `merge_translations` - Merge Reviewed Translations
（保留現有說明，強調 autoCommit/autoPush 功能）

### 6. `cleanup_diff_directory` - Clean Up Diff Directory
（保留現有說明）

### ~~git_commit_push~~ (已移除於 v1.3.0)
此工具已移除，請使用標準 git 指令或其他工具的整合 git 功能。
```

### 7.3 更新 CHANGELOG.md

```markdown
# Changelog

## [1.3.0] - 2025-11-13

### Removed

- ❌ `git_commit_push` 工具 - 請使用標準 git 指令替代

### Rationale

移除 `git_commit_push` 的原因：
- Git 操作過於基礎，開發人員已熟悉標準指令
- `generate_locale_diff` 和 `merge_translations` 已整合 git 功能
- 簡化 API 表面積，專注於翻譯相關功能

### Migration

**舊方式：**
```json
{"tool": "git_commit_push", "params": {...}}
```

**新方式：**
```bash
git add <files>
git commit -m "message"
git push origin <branch>
```

或使用其他工具的整合功能：
```json
{
  "tool": "generate_locale_diff",
  "params": {
    "autoCommit": true,
    "autoPush": true
  }
}
```

### Retained

- ✅ 所有 6 個核心和工作流程工具保持不變
- ✅ generate_locale_diff 和 merge_translations 的 git 整合功能

---

## [1.2.2] - 2025-11-05
（保留之前的條目）
```

## 8. 風險與緩解

### 8.1 使用者影響風險

#### 風險 1：使用 git_commit_push 的使用者需要調整
**影響**：低 - 僅影響使用此特定工具的使用者
**可能性**：低 - 大多數使用者可能使用標準 git 指令
**緩解**：
- ✅ 提供清晰的替代方案（標準 git 指令）
- ✅ 指出其他工具的整合 git 功能
- ✅ 在 CHANGELOG 中清楚說明
- ✅ 這是次要版本變更，不是重大變更

#### 風險 2：自動化腳本需要更新
**影響**：低 - 僅影響使用 git_commit_push 的腳本
**可能性**：低 - 預期使用率不高
**緩解**：
- ✅ 更新後的版本號清楚標示變更 (1.3.0)
- ✅ CHANGELOG 提供遷移說明
- ✅ 替代方案簡單直接

### 8.2 技術風險

#### 風險 3：移除過程中引入錯誤
**影響**：低 - 工具間沒有相依性
**可能性**：極低 - git_commit_push 是獨立工具
**緩解**：
- ✅ 移除後執行完整測試
- ✅ 使用 typecheck 驗證
- ✅ 使用 MCP Inspector 測試
- ✅ git 保留歷史記錄

#### 風險 4：遺漏清理相關程式碼
**影響**：極低 - 不影響功能
**可能性**：低
**緩解**：
- ✅ 檢查未使用的 import
- ✅ 檢查未使用的相依性
- ✅ 執行 linting 檢查

### 8.3 專案影響風險

#### 風險 5：功能覆蓋度減少
**影響**：極低 - git 功能已整合到其他工具
**可能性**：不適用
**緩解**：
- ✅ generate_locale_diff 包含 autoCommit/autoPush
- ✅ merge_translations 包含 autoCommit/autoPush
- ✅ 使用者仍可使用標準 git 指令
- ✅ 實際功能沒有損失

## 9. 成功標準

### 9.1 技術標準

- ✅ 伺服器可以成功建置（`npm run build`）
- ✅ 通過類型檢查（`npm run typecheck`）
- ✅ MCP Inspector 顯示 6 個工具（不含 git_commit_push）
- ✅ 所有 6 個保留的工具功能正常
- ✅ 沒有未使用的 import 或相依性
- ✅ 程式碼庫減少約 3 KB

### 9.2 文件標準

- ✅ README.md 已更新且不提及 git_commit_push
- ✅ CLAUDE.md 已更新工具清單
- ✅ CHANGELOG.md 已新增條目
- ✅ 所有文件使用繁體中文
- ✅ 提供清晰的替代方案說明

### 9.3 功能保留標準

- ✅ 所有翻譯功能完全正常
- ✅ 所有工作流程功能完全正常
- ✅ 整合的 git 功能（autoCommit/autoPush）正常
- ✅ 使用者可以用標準 git 指令替代

## 10. 時程規劃

### 階段 1：實施（0.5 天）
- ⏳ 移除工具註冊和檔案（15 分鐘）
- ⏳ 更新所有文件（30 分鐘）
- ⏳ 執行測試和驗證（1 小時）

### 階段 2：審核和提交（0.5 天）
- ⏳ 審核所有變更（30 分鐘）
- ⏳ 建立提交和推送（15 分鐘）
- ⏳ 更新版本號和 CHANGELOG（15 分鐘）

**總計時間：** 0.5-1 天

## 11. 決策記錄

### 為什麼只移除 git_commit_push？

1. **獨立性**：
   - git_commit_push 是完全獨立的工具
   - 與其他工具沒有相依關係
   - 移除它不影響任何其他功能

2. **重複性**：
   - generate_locale_diff 已有 autoCommit/autoPush
   - merge_translations 已有 autoCommit/autoPush
   - 獨立的 git 工具是重複的

3. **價值低**：
   - 只是包裝標準 git 指令
   - 沒有提供額外價值
   - 開發人員已熟悉 git 指令

4. **保留其他工具**：
   - generate_locale_diff 提供獨特價值（diff 產生）
   - merge_translations 提供獨特價值（智慧合併）
   - cleanup_diff_directory 提供方便性

### 為什麼是次要版本（1.3.0）而非重大版本（2.0.0）？

1. **影響範圍小**：
   - 僅移除 1 個工具（共 7 個）
   - 核心翻譯功能完全不受影響
   - 工作流程工具完全保留

2. **功能可替代**：
   - 有清晰的替代方案
   - 功能沒有真正損失
   - 只是改變使用方式

3. **語義版本考量**：
   - 移除不常用的工具
   - 核心 API 和功能保持穩定
   - 符合次要版本的定義

---

## 總結

本規格文件定義了移除 `git_commit_push` 工具的計劃。這是一個小幅度的簡化變更，旨在：

1. **移除重複功能**：git 功能已整合到其他工具
2. **簡化 API**：減少一個不必要的工具
3. **專注核心價值**：翻譯和工作流程管理

### 關鍵要點

- ❌ **移除 1 個工具**：git_commit_push
- ✅ **保留 6 個工具**：所有翻譯和工作流程工具
- 🔢 **版本 1.3.0**：次要版本變更
- 📚 **簡單遷移**：使用標準 git 指令或整合功能
- ⏱️ **快速實施**：0.5-1 天

### 影響評估

- **使用者影響**：極小（僅影響使用 git_commit_push 的少數使用者）
- **功能影響**：無（功能可完全替代）
- **維護影響**：正面（減少維護負擔）
- **風險等級**：極低

### 下一步

1. ✅ 規格已完成
2. ⏳ 使用 `/speckit-plan` 建立實作計劃（可選，因為很簡單）
3. ⏳ 執行實施
4. ⏳ 發布 v1.3.0

---

**規格準備者：** Claude Code (AI 助手)
**日期：** 2025-11-13
**狀態：** 規劃中 - 等待批准
**估計工時：** 0.5-1 天
**優先級：** 低
**風險等級**：極低
**版本號**：1.3.0 (次要版本)
